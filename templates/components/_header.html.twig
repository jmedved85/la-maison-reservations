{% block header %}
    {# Navigation #}
    <nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_home') }}">
                La <span class="brand-accent">Maison</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_home') }}">
                            <i class="bi bi-house-door me-1"></i>&nbsp;{{ 'home'|trans }}
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_home') }}#reservation">
                            <i class="bi bi-calendar-check me-1"></i>&nbsp;{{ 'reservations'|trans }}
                        </a>
                    </li>
                    {# <li class="nav-item">
                        <a class="nav-link" href="/admin">
                            <i class="bi bi-gear me-1"></i>&nbsp;{{ 'admin'|trans }}
                        </a>
                    </li> #}
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}